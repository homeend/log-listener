# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Build & Run

```bash
go build -o log-listener .
go run .
```

## Project Overview

Go module `log-listener` (Go 1.26). Currently a single-file project (`main.go`) at the package `main` level with no external dependencies.
